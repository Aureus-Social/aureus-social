'use client';
import { useLang } from '../lib/lang-context';
import { useState } from 'react';
export function DashDelegation({s,d}){
  const { t, lang } = useLang();return <div style={{padding:40,textAlign:'center',color:'#5e5c56'}}>Module DashDelegation</div>;}
export function EgaliteHF({s,d}){
  const { t, lang } = useLang();return <div style={{padding:40,textAlign:'center',color:'#5e5c56'}}>Module EgaliteHF</div>;}
export function GestionElections({s,d}){
  const { t, lang } = useLang();return <div style={{padding:40,textAlign:'center',color:'#5e5c56'}}>Module GestionElections</div>;}
export function PlanDiversite({s,d}){
  const { t, lang } = useLang();return <div style={{padding:40,textAlign:'center',color:'#5e5c56'}}>Module PlanDiversite</div>;}
export function RapportCE({s,d}){
  const { t, lang } = useLang();return <div style={{padding:40,textAlign:'center',color:'#5e5c56'}}>Module RapportCE</div>;}
export function TableauSeuils({s,d}){
  const { t, lang } = useLang();return <div style={{padding:40,textAlign:'center',color:'#5e5c56'}}>Module TableauSeuils</div>;}


export default DashDelegation;
